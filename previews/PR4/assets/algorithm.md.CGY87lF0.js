import{_ as t,o as a,c as i,ao as r}from"./chunks/framework.fZctpB7K.js";const u=JSON.parse('{"title":"Algorithm","description":"","frontmatter":{},"headers":[],"relativePath":"algorithm.md","filePath":"algorithm.md","lastUpdated":null}'),o={name:"algorithm.md"};function n(s,e,h,l,d,c){return a(),i("div",null,[...e[0]||(e[0]=[r('<h1 id="Algorithm" tabindex="-1">Algorithm <a class="header-anchor" href="#Algorithm" aria-label="Permalink to &quot;Algorithm {#Algorithm}&quot;">​</a></h1><p>BOCPD maintains competing hypotheses about how long the current segment has lasted. For the sequence <code>10.1, 9.8, 10.2, 10.0, 15.1, 14.9</code>, the fifth value may be an outlier or evidence for a new regime. The filter keeps both possibilities until later data resolve some uncertainty.</p><h2 id="Run-length" tabindex="-1">Run length <a class="header-anchor" href="#Run-length" aria-label="Permalink to &quot;Run length {#Run-length}&quot;">​</a></h2><p>The run length is</p><p>[ r_t = \\text{number of observations since the most recent changepoint}. ]</p><p>BOCPD.jl uses <code>r_t = 0</code> when <code>x_t</code> is the first observation of a new segment. Each active run length has its own posterior state and predictive distribution.</p><h2 id="Recursion" tabindex="-1">Recursion <a class="header-anchor" href="#Recursion" aria-label="Permalink to &quot;Recursion {#Recursion}&quot;">​</a></h2><p>Let <code>H(r)</code> be the hazard, the prior probability that the next observation starts a new segment. For an existing run-length hypothesis, the growth branch is</p><p>[ p(r_t=r+1, x_t) = p(r_{t-1}=r) [1-H(r)] p(x_t \\mid r). ]</p><p>The reset branch collects all possible previous run lengths:</p><p>[ p(r_t=0, x_t) = \\sum_r p(r_{t-1}=r) H(r) p(x_t \\mid r=0). ]</p><p>The reset predictive uses a fresh prior state. Growth uses the predictive density of the existing posterior state and then updates that state. After both branches are constructed, weights are normalized.</p><p>For a constant hazard <code>h</code>, <code>ConstantHazard(lambda)</code> uses <code>h = 1 / lambda</code>. This is a geometric duration prior with expected duration <code>lambda</code>, not a deterministic interval.</p><h2 id="Numerical-stability" tabindex="-1">Numerical stability <a class="header-anchor" href="#Numerical-stability" aria-label="Permalink to &quot;Numerical stability {#Numerical-stability}&quot;">​</a></h2><p>Weights are stored as logarithms. Sums over competing hypotheses use log-sum-exp, preventing underflow on long sequences or highly surprising observations.</p><h2 id="Missing-observations" tabindex="-1">Missing observations <a class="header-anchor" href="#Missing-observations" aria-label="Permalink to &quot;Missing observations {#Missing-observations}&quot;">​</a></h2><p>A fully missing observation has log predictive contribution zero. Time still advances and the hazard transition still occurs:</p><p>[ \\log p(x_t=\\text{missing}\\mid state)=0. ]</p><p>Growth carries the posterior state unchanged and reset creates a fresh prior state. It is therefore not equivalent to skipping the time point or imputing a value.</p><h2 id="Pruning" tabindex="-1">Pruning <a class="header-anchor" href="#Pruning" aria-label="Permalink to &quot;Pruning {#Pruning}&quot;">​</a></h2><p>Exact filtering grows with the number of observations. <code>MaxRunLength</code>, <code>ProbabilityPruning</code>, and <code>TopKPruning</code> bound the active hypotheses. Retained weights are renormalized and discarded mass is recorded. These strategies are approximations.</p><h2 id="Fixed-lag-smoothing" tabindex="-1">Fixed-lag smoothing <a class="header-anchor" href="#Fixed-lag-smoothing" aria-label="Permalink to &quot;Fixed-lag smoothing {#Fixed-lag-smoothing}&quot;">​</a></h2><p>Later observations can revise whether an earlier observation began a segment. <code>changepoint_probability(result, t, FixedLag(L))</code> computes a marginal over latent transition histories conditioned on data through <code>t + L</code>. It is not generally the same as the terminal event <code>r_(t+L) == L</code>, which excludes paths containing later changepoints.</p>',23)])])}const g=t(o,[["render",n]]);export{u as __pageData,g as default};
